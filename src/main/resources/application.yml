spring:
  datasource:
      url: jdbc:mysql://192.168.9.240:3306/lms?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: lms
      password: lmsZdWI1o
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.wangff.learning.api.model
  mapper-locations: classpath:mapper/*.xml

#pagehelper
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql